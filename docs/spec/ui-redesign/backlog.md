# UI 重做 · Backlog

设计稿里**画了、但应用还没有那个能力**的部分。按任务要求，它们一律先以设计稿的
「占位」形态落地（画出来但禁用 / 说明未实现），能力本身记在这里。

同时记录设计稿与现有行为**冲突**的地方，以及裁决的理由 —— 重做只动表现层，
凡是会改变行为的差异都停在这份清单上，不在代码里悄悄改掉。

---

## 一、未实现的能力（已按占位落地）

| # | 来源 | 设计稿要的 | 现在的样子 | 缺什么 |
|---|---|---|---|---|
| B1 | 03.2 | **媒体库分区**：海报网格、筛选 chips、剧集详情页 | `_LibraryPlaceholder` —— 走完整卡片与令牌规范的占位卡 | 一个媒体库模型（扫描结果的持久化、海报缓存、按类型/状态筛选）。目前应用只有「当前文件夹」这一个概念 |
| B2 | 03.3 | 空状态的**「连接 NAS」** | 按钮画出来但禁用，tooltip 写「即将推出」 | 应用只做本地文件系统操作；连 NAS 意味着挂载管理与凭据存储 |
| B3 | 05.1 | **命名规则编辑器**可编辑模板（`{title} {year}` 拖拽） | `rename_rule_dialog` 只读展示内置约定 + 实时预览 | 路径由 `JellyfinNaming` 按规则生成，没有用户模板系统。引入模板要同时改 organize agent 的契约（模型仍不得命名路径） |
| B4 | 05.2 | 预览的**「海报视图」**第三视图 | 只有列表；视图切换器暂不提供第三段 | 需要 B1 的海报来源 |
| B5 | 05.4 | **`⌘Z / Ctrl+Z` 直接撤销上一条**，配顶栏下方 toast | 未实现；浮层页脚改为陈述条数，不写这个键位 | 需要一个新快捷键 + 「最近一条是否可撤销」的判定。新增快捷键属于功能改动，不在本次表现层重做的范围 |
| B6 | 06.1 | AI 服务详情的**用量条**（本月金额 / 请求数 / 平均延迟 / 柱状图） | 版式保留，能填的填，其余显示 `—` | `AiService` 只累计 token 与条目数，没有计费、延迟直方图 |
| B7 | 06.2 | **多媒体库根目录**、挂载状态、「重新扫描」 | 路径页按设计稿画出整块，然后整体压暗 + 「即将推出」标签；真实的只有收藏与最近 | 同 B1 |
| B8 | 06.2 | 收藏 8 条上限 + 最近 **20** 条 | 沿用 `SettingsService` 现有的 recents 上限 8 | 改上限会动 `config.json` 的行为，留给单独一次改动 |
| B9 | 06.2 | 隐私分区的**匿名统计 / 崩溃报告 / 记录 AI 正文 / 退出清临时目录** | 四个开关按设计稿画出来，全部关闭且不可拨，卡底写明「本应用不采集任何遥测数据」 | 引入遥测是产品决定，不是 UI 决定 |
| B10 | 06.2 | 隐私分区的 **`library.db` 与日志导出** | 只列实际存在的缓存（缩略图、撤销 blob、agent 工作区） | 应用没有数据库；运行日志目前只在内存与任务面板里 |
| B11 | 06.2 | 快捷键**可改绑** + 冲突提示 | 搜索框是真的（同时筛命令名与键位文本）；「恢复默认」画出来但禁用，表格仍从 `app_shortcuts.dart` 渲染 | `AppShortcuts` 是编译期单一事实来源；改绑需要一层持久化覆盖 |
| B12 | 06.4 | 关于页的**提交 / 分支 / 提交时间** | 构建号已有（从 `_appVersion` 的 `+N` 拆出）；提交与分支两行显示 `—` 并在卡底说明原因 | 需要构建期注入（`--dart-define` 或生成文件） |
| B13 | 03.1 | 侧栏行尾的**条目计数**（「电影 238」） | 不显示 | 按目录计数要么每次进侧栏 stat 整棵树，要么维护一份会过期的缓存 —— 两者都比这个数字贵 |
| B14 | 02.5 | macOS 交通灯**垂直居中于 48px 顶栏** | 已在 `MainFlutterWindow.swift` 手动摆放 | **未在 macOS 上实测过**（本次开发环境只有 Windows）。首次 mac 构建时需要核对 |
| B15 | 02.2 | Windows **Snap Layouts** | 代码写了，但 **2026-09-12 在 Win11 26200 真机上验证为不工作**：悬停 14 秒无浮层。判据可信 —— 同一脚本、同样的合成悬停下，msinfo32（系统标题栏）与 Windows Terminal（自绘标题栏）都能弹出 | 两个已定位的 bug 加一个未知。① `HasCustomTitleBar()` 判 `WS_CAPTION` 是否已清除，而 `window_manager` 的 `TitleBarStyle.hidden` **从不碰 `GWL_STYLE`**（它靠 `WM_NCCALCSIZE` 把客户区铺满窗口），所以该判断恒为假，整个 `WM_NCHITTEST` 分支是死代码 —— 实测原样构建 8 个探测点全 `HTCLIENT`，把判断短路成 `true` 后 `HTMAXBUTTON` 8/8。② `MaximizeButtonRect` 在非最大化态偏右 8px：`window_manager` 对客户区做 `right -= 8`（**物理像素，不随 DPI**），代码只在 `IsZoomed` 时补 inset，且补的是 `8 * scale`，而最大化时真实溢出量是 `window.left - workArea.left`（200% 下为 13）。③ 补上 ① 之后浮层**仍然不出现**，让 `WM_NCMOUSEMOVE` 落到 `DefWindowProc` 也无效，原因未明；最可疑的是 `MessageHandler` 里 `flutter_controller_->HandleTopLevelWindowProc()` 排在我们的 `switch` 之前，真实输入下可能已被引擎消费。**用户可见的症状只有「没有 Snap 浮层」**：原生路径全程失效，输入按普通客户区走，悬停高亮与点击由 Flutter 自己接管，都正常 |
| B16 | 06.2 / 08b | 取色浮层的**透明度轨** | 画出来但不可拖 | 令牌层存的是一个**不透明**强调色，标签页底、焦点环、进度条都从它乘一个 alpha 推导出来。存一个自己就带 alpha 的强调色，等于每处推导再乘一次，对比度随之塌掉 |
| B17 | 06.2 / 08b | 取色浮层的**屏幕吸管** `⌖` | 画出来但不可点 | 抓屏幕像素需要 Windows / macOS / Linux 各一套原生实现 |
| B18 | 06.2 | 路径页的**收藏拖拽排序** `⋮⋮` 与「清除记录」 | 手柄与按钮画出来但不可用 | `SettingsService` 的 favorites / recents 没有重排与清空接口；加接口属于行为改动 |
| B19 | 06.2 | **重置所有数据**（输入 `RESET` 二次确认、可保留 AI 配置） | 危险区整块画出来但禁用，卡底写明先手动删配置夹 | 一个不可撤销的删除路径，不应该作为表现层重做的附带品上线 |
| B20 | 06.4 | 关于页的**更新日志 / 检查更新** | 两颗按钮画出来但禁用 | 应用没有更新渠道；接上它意味着一个发布清单源与版本比较 |
| B21 | 06.4 | 关于页的**第三方许可**（24 画的是「47 个依赖」可点进去） | 该行画出来但压成「即将推出」 | 依赖清单要一个能翻的整页；而整页路由必须自带二级顶栏，`showLicensePage` 直接推会让窗口失去拖拽区和窗口按钮 |
| B22 | 1.2 | 设计稿点名的等宽字体 **JetBrains Mono** | 没有打进包里；`AppTypeScale.monoFallback` 依次退到 Cascadia Mono / Consolas / SF Mono / Menlo / DejaVu Sans Mono | 打包字体是一个许可与体积的决定（OFL，四个字重约 800 KB），不该顺手塞进一次表现层修复里。装了 JetBrains Mono 的机器现在就能用上它 |
| B23 | 06.2 | 22 新加的**偏好设置备份**（导入 / 导出） | 行按设计稿画出来，两颗按钮压暗 | 应用没有导入导出偏好的通道。眼下能做的是复制上一行指的那个配置夹 —— 一个真的导入要先决定「导入的 `ai_profiles.json` 里那些密钥算谁的」，那不是一次表现层改动该顺手定的事 |
| B24 | 06.2 | 21 新加的 **NFO 与图片写入位置**可改 | 行按设计稿画出来，值是真的（NFO 与图片就写在媒体文件旁边），「更改」压暗 | 写入位置由 `MetadataWriter.nfoNameFor` 按 Jellyfin 的规矩推出来，不是一个设置项；把它变成设置项要同时想清楚混合目录下的那条分支 |
| B25 | 03.1 | 文件复制 / 剪切 / 粘贴与**系统剪贴板**互通（从 Finder / 资源管理器复制的文件能粘贴进来，反之亦然） | 应用内剪贴板已实现：⌘C / ⌘X / ⌘V、右键菜单、页脚「已剪切 N 项 · 粘贴到这里」，只在本应用内生效 | Flutter 没有跨平台的文件剪贴板 API；`CF_HDROP`、`NSFilenamesPboardType`、`text/uri-list` 三种格式各需一套原生代码 |
| B26 | 03.1 | 粘贴时的**覆盖 / 合并**选项（同名文件夹合并内容、同名文件替换） | 冲突对话框只给「跳过」与「两者都保留」 | 应用其他所有写盘路径（整理、撤销）都拒绝覆盖，粘贴不该成为唯一一个没有二次确认、没有撤销的破坏性动作；合并两棵树还要定义子项冲突的规则 |
| B27 | 03.1 | 文件表内**拖拽**到文件夹行 / 侧栏收藏即移动 | 未实现 | 拖拽是第二条进入 `executeTransfer` 的路，需要拖拽预览与放置高亮两套表现，先让键盘与菜单路径稳定下来 |
| B28 | AI 接入 · 02 | 添加渠道时的**预置模型**（可勾选） | 不预置任何模型名；渠道页用「添加模型」手填上游名 | 模型名随厂商频繁变化，编进包里的名单很快过期，且「没有编造的预设」是本项目的规矩。要做就从渠道的 `/models` 列表实时拉取 |
| B29 | AI 接入 · 03 | 每条线路**单独选鉴权方式**（Bearer / x-api-key / 两者） | 鉴权由协议决定：兼容线路 Bearer、Gemini `x-goog-api-key` | 需要实测哪些中转站要求哪种头；在数据里加 `RouteSpec.auth` 之前不暴露一个猜出来的开关 |
| B30 | AI 接入 · 01 | 总览右下的**本月用量**（含缓存命中、「未上报」计数） | 显示「本次启动以来」的请求数、tokens 与平均延迟 | 用量没有持久化；要按月累计得先有一个用量账本（API 日志不适合当账本：默认关闭） |
| B31 | AI 接入 · 03 | 密钥存进**系统钥匙串** | 仍明文存 `ai_profiles.json`（与此前相同） | 审查报告的待决问题之一：引入 `flutter_secure_storage`，Linux 依赖 libsecret |

## 二、与现有行为冲突，裁决在此

| # | 冲突 | 裁决 |
|---|---|---|
| C1 | 03 的画板仍画着拆分前的旧外壳（28px 系统标题栏 + 50px 应用工具条、侧栏 236–240） | **外壳一律以 02 为准**（统一 48px、侧栏 244、右面板 352）。03 只取主内容区规格 |
| C2 | 05.4 浮层与独立窗口写「保留最近 30 天」，抽屉写「保留 7 天」 | 以 `HistoryService.retentionDays = 7` 为准 —— 文案必须说真话 |
| C3 | 05.2 页脚勾选项写「同时备份原文件夹快照」 | 应用的 `backup` **不复制任何东西**，它只决定是否记录撤销清单。沿用现有 l10n 文案，只取设计稿的版式 |
| C4 | 04.3 的图片角色菜单写死了 poster / fanart / folder / thumb 四项 | 以 `ImageRole` 枚举为准，且**每个角色只出现一次** —— 两个角色映射到同一个 Jellyfin 类型会互相覆盖 |
| C5 | 1.3b 要求窗口圆角 14–18 | 不在 Flutter 侧裁剪：两个系统对无边框窗口本来就自己倒角，再套 `ClipRRect` 只会在四角露出不透明底色 |
| C6 | 1.4e 的弹层出现动画与 Material 的缩放入场 | 按 1.4f「不做缩放、不做弹性曲线」，全部改成淡入 + ≤4px 位移 |
| C7 | 6.x 的左导航只有**七项** | 应用多一项「刮削」（Cookie 与规则库）。它是真实在用的能力，为了对齐导航长度把它藏起来是本末倒置；它沿用同一套 6.x 骨架 |
| C8 | 6.2 缓存分为「元数据 / 图片 / 运行日志」三档 | 按**磁盘上真实存在的三样东西**分：缩略图 / 撤销备份 / 整理记忆。一张列出三个不存在目录的缓存表，比没有这张表更糟 |
| C9 | 6.2 每一行缓存都带「清理」 | **撤销备份那一行不给**：它装的是撤销时用来还原被覆盖文件的真副本，删掉就把还没过期的撤销记录变成空头支票。`HistoryService` 已按 7 天自动清理，这里只报大小 |
| C10 | 03b 模型参数页右上的**保存**按钮 | 按设计稿保留（收起 + 保存，齐右、间距 8）。页内每一处改动本来就落键即存，所以这颗按钮不是这些值唯一的出路 —— 它把还停在输入框里的那一处提交上去、再写一次、并给一句回执，好让人不必去猜到底存没存 |
| C11 | 1.3a 的间距刻度里没有 18 / 26 / 9，而 06 的卡片反复用它们 | 一律吸附到最近的刻度（18→16、26→28、9→8）。整套界面统一到刻度上，比逐处复刻更贴近「规格统一」这个要求本身 |
| C12 | 24 的旁注写「单 GPU 设备隐藏此项」 | **不隐藏**。`GpuInfo` 存在的理由就是回答「Windows 把哪块卡给了这个应用」，那正是改完「按应用设置显卡」之后要来核对的一行；机器上只有一块时它依然是答案，藏掉等于把这个诊断删了。「检测到 N 个」徽标倒是只在多于一块时才挂 —— 「检测到 1 个」什么也没说 |
| C13 | 24 的开源卡列「第三方许可」，没有「Jellyfin 命名规范」 | 两行都留。命名规范是这个应用整理文件所依据的那份文档，是它已经在提供的链接，为了对齐行数删掉它是本末倒置 |
| C14 | 21 / 22 的短卡用 `justify-content:space-between` 把行铺满行轨道 | **22 照做，21 的「默认位置」不照做**。设计稿是在「最近访问只有 4 条」的内容量下均分的；应用的最近访问最多 8 条，同一条行轨道能有设计稿的两倍高，三行均分之后行与行相隔一百多像素，卡看上去是坏的而不是宽松的。那一格改成行照旧顶排、说明钉在卡底，余量落在两者之间 ——和它上面的「收藏的路径」同一种处理。22 的两张短卡内容是定长的，余量小且可预期 |

## 三、本次重做主动补的、设计稿没画的

设计稿是按整页画的，没有覆盖「一个整页路由盖住主顶栏之后会怎样」。

- **二级顶栏**（`SecondaryTitleBar`）：隐藏系统标题栏之后，`Navigator.push` 的整页
  界面会连拖拽区和窗口按钮一起盖掉 —— 设置、历史、进度页都会变成一个既不能移动
  也不能关闭的窗口。三个页面现在戴同一条 48px。
- **状态栏分工**：3.1 的面板页脚和 2.6 的状态栏都要写计数，两者对「已选」的口径
  不同（面板把聚焦行算作 1）。状态栏改为只讲总大小与 AI 状态。
